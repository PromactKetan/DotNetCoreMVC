@model IEnumerable<EmployeeManagement.Models.Department>

@{
    ViewData["Title"] = "Index";
}

<h2>Department List</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Departments)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Deescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Departments)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Deescription)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.DepartmentID" class="btn btn-success"><i class="glyphicon glyphicon-pencil"></i>Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.DepartmentID" class="btn btn-info"><i class="glyphicon glyphicon-menu-hamburger"></i> Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.DepartmentID" class="btn btn-danger"><i class="glyphicon glyphicon-trash"></i>Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
